# AI 编程线程管理与上下文使用指南

适用场景：使用 AI 编程助手持续开发包含多个模块的项目。本文中的“线程”也指对话、会话或任务，不依赖特定工具。

本文以 YUME 的工作日志、日报周报、角色包和长期记忆功能为例。功能背景来自当前项目说明；开发提问和故障情境用于演示线程管理，不代表已经发生的故障或已完成的验证。

## 1. 推荐原则

**按模块组织工作，按一项连续任务划分线程。**

一个模块可以有多个线程，不必永久绑定一个线程。同一个功能从需求讨论、实现、测试到修复，适合在同一线程中完成；进入另一个独立功能时，可以新开线程。

例如，在 YUME 中完成“从聊天消息保存工作事项，并在工作日志中查看”，需要确认保存条件、接通聊天操作与本地存储、展示保存回执，再验证工作日志中的记录。这些步骤服务于同一个目标，适合在同一线程中完成。

如果保存事项已经验收，下一项任务是“每周五 17:00 自动生成周报”，虽然仍属于工作日志模块，但涉及新的调度目标，可以新开线程。

建议让一个线程处理一个连贯的工作单元。如果仍在解决同一个问题，继续原线程有助于保留推理过程和决策背景。

## 2. 同一线程为什么有帮助

需要区分上下文延续和缓存命中。

### 文档上下文延续

原线程能够延续此前讨论过的需求、约束、设计取舍和排查经过，减少重复解释。这通常是继续使用同一线程最直接的收益。

例如，开发 YUME 工作日志时，需要保留这些项目约束：

- 工作事项与报告使用独立的本地数据库，并由所有角色共享。
- 删除聊天记录不应删除已归档的工作记录。
- 后台报告使用独立会话，不打断当前聊天。
- 重新生成报告时，应保留人工编辑的版本，等待用户选择是否采用新版本。

假设验收时发现“重新生成日报覆盖了手工修改”，这仍属于报告版本处理的原任务。继续原线程，可以结合此前确定的保留规则、实现改动和复现步骤排查，不必重新解释整个工作日志功能。

不同工具可能对长对话进行摘要、压缩或截断，也可能要求手动整理。界面中保留的聊天历史，不代表模型每次都能完整读取，因此不要把聊天历史当作唯一的长期项目记录。

### 缓存命中

如果工具支持提示词前缀缓存，复用通常依赖请求前部相同的内容，而不是模块名称或线程名称。其他缓存机制可能采用不同规则，应以所用工具及模型服务的说明为准。

例如，在“YUME 工作日志｜日报版本保留”线程中，连续追加实现要求、验证结果和修复要求，如果实际请求前部保持相同，并满足服务的缓存条件，可能有利于缓存复用。把新线程也命名为“YUME 工作日志”，并不会仅凭名称获得这些收益。

对于支持前缀缓存的系统，在原线程中连续追加消息、保持前面的内容稳定，可能有利于缓存复用。但同一线程并不保证命中缓存，新线程也不必然完全无法复用缓存。

- 缓存可能设有保留期限，隔几天返回老线程，不代表之前的缓存仍然存在。
- 上下文压缩、摘要或截断可能改变请求前缀，影响缓存复用。
- 实际命中还受模型、请求路由和缓存是否仍可用等因素影响。

**缓存命中率高，不等于总消耗更低。** 很长的线程携带大量历史内容，即使部分命中缓存，也可能比精简的新线程消耗更多。

例如，工作日志验收结束后，再在同一线程处理角色包导入，会携带大量报告生成、调度和版本编辑的历史。这些内容与角色包任务关系不大，不值得仅为缓存保留。

模型 API 的缓存价格，也不能直接换算成编程助手的订阅额度；应查看具体计费规则和实际用量。

## 3. 什么时候继续，什么时候新开

| 情况 | YUME 开发场景 | 建议与原因 |
| --- | --- | --- |
| 同一个功能，继续实现、测试或修复 | 保存工作事项后，检查聊天回执与工作日志中的内容是否一致 | 继续原线程，保留需求与排查背景 |
| 同一个模块，开始另一个独立功能 | 事项保存已验收，开始处理定时周报 | 新开线程，让上下文围绕调度与执行目标展开 |
| 当前问题尚未解决，但线程已经很长 | 假设报告任务一直处于等待状态，已经排查了多轮 | 优先保持任务连续，必要时整理复现步骤、日志摘要、已排除原因和待验证假设后交接 |
| 一个阶段完成，准备进入下一阶段 | 工作日志实现阶段结束，进入独立的桌面验收阶段 | 记录交接信息后新开线程，提供当前实现、验收场景与未验证项；连续的小范围测试仍可留在原线程 |
| 任务跨越多个模块，但服务于同一个明确目标 | 从聊天中记录工作事项，到本地保存，再到设置页查看 | 可以放在同一线程，以完整结果划分，而非机械地按前端、后端目录拆分 |

## 4. 实际示例

### 场景一：完成聊天保存工作事项

线程名称：`YUME 工作日志｜聊天保存事项与验收`。

可以这样开始：

> 本次目标是核对并完善“从聊天保存工作事项”的完整流程。请先阅读现有工作日志说明及相关代码，检查明确记录请求、保存回执、本地持久化和设置页展示。验收时确认普通聊天不会自动新增事项，并检查重启后记录是否仍在。

如果验收发现聊天显示保存成功，但工作日志里没有对应事项，应在本线程继续排查。需求、保存入口和验证过程仍然相关，无须按聊天界面、数据库、设置页分别新开线程。

### 场景二：切换到定时周报任务

事项保存验收后，可以新开：`YUME 工作日志｜定时周报与重启补执行`。

新线程需要读取工作日志的稳定约束，再围绕自己的目标展开：计划时间、应用运行条件、暂停计划、重启后补执行，以及生成失败后的状态反馈。

例如：

> 本次目标是核对并完善定时周报。请先检查现有调度实现，重点验证每周五 17:00 的计划、暂停后的行为，以及应用重启后只补执行最近一次错过的计划。报告应使用独立会话，避免影响当前聊天。

这是同一模块中的独立任务，可以通过文档继承必要背景，不必携带事项保存过程中的全部调试历史。

### 场景三：跨模块完成角色包导入

线程名称：`YUME 角色包｜导入后选择与加载验收`。

这项任务可能同时涉及 `.dmpack` 解包、包元数据、设置页角色列表和桌宠资源加载。但目标只有一个：用户导入角色包后，能够选择并正常显示包内角色。

因此，导入、选择、加载以及本次验收发现的相关修复可以在同一线程中完成。若该功能早已验收，之后单独出现导入故障，再为这个独立问题新开排查线程。

推荐使用“模块名称 + 本次目标”命名，方便查找：

```text
YUME 工作日志｜聊天保存事项与验收
YUME 工作日志｜日报人工版本保留
YUME 工作日志｜定时周报与重启补执行
YUME 角色包｜导入后选择与加载验收
YUME 长期记忆｜共享与角色专属记忆隔离验证
```

## 5. 如何保存模块的长期记忆

**线程负责当前任务的连续性，项目文档负责模块的长期记忆。**

这里的“长期记忆”指开发知识的保存，与 YUME 面向用户的长期记忆功能是两件事。不能依赖桌宠记住用户偏好的功能，替代项目设计说明和开发交接文档。

将稳定且有复用价值的信息写入项目文档，例如模块说明、设计决策记录；将需要持续遵守的开发规则写入所用工具支持的项目规则文件。避免把全部聊天记录堆入规则文件。

以 YUME 为例，“删除聊天不删除工作记录”“后台报告不打断当前聊天”“人工版本需要保留”适合写入模块说明。一次调试中的完整日志不必全部写入长期规则；保留必要证据及其位置即可。

阶段结束时，建议记录：

1. 已完成的功能与当前状态。
2. 关键入口文件和模块职责。
3. 已确定的设计决策及原因。
4. 已执行的验证与结果。
5. 尚未解决的问题及下一步。

工作日志模块的交接可以采用以下模板。路径来自当前项目，状态与结果必须根据当次开发实际填写：

```text
本次目标
【例如：验证日报重新生成时保留人工编辑版本】

已完成内容与当前状态
【填写实际改动；区分已实现、已测试、已在桌面应用验收】

相关入口
README.md：工作日志功能与使用约束。
src/settings/worklog/：工作日志设置界面。
src/lib/worklog.ts：前端工作日志接口。
src-tauri/src/worklog/：后端工作日志实现。
scripts/worklog-qa/desktop-scenarios.md：桌面验收场景。

需延续的约束
重新生成报告时保留人工编辑版本，等待用户选择是否采用候选版本。
后台报告使用独立会话，不打断当前聊天。

验证与证据
【填写实际执行的场景、结果和证据位置；未执行项明确标注】

遗留问题与下一步
【记录复现步骤、已验证事实、待验证假设及下一步】
```

可直接在当前线程发送：

> 请把本阶段 YUME 工作日志的已完成内容、关键文件、设计决策、验证结果和待办整理成项目交接文档，供下一线程读取。区分已验证事实和仍需确认的事项，明确哪些场景已在桌面应用中操作验证，哪些仅通过了自动测试。

新线程开始时可以发送：

> 本次目标是继续完成 YUME 日报版本保留的验收。请先阅读【交接文档实际路径】、README.md 的工作日志说明和相关代码，核对当前实现，再执行尚未完成的场景。重点检查人工编辑后重新生成日报，以及选择采用候选版本的行为。

交接文档应反映实际进展。项目里有验收脚本，不代表本阶段已经执行并通过；界面上出现“成功”，也不应替代对保存结果的核对。

## 6. 使用优先级

优先保证任务边界清晰、上下文相关、结果可验证，再考虑缓存收益。不要仅为了追求缓存命中率而无限延长线程。

放到 YUME 开发中：工作事项保存还没验收完，就继续推进这项任务；切换到角色包导入，就围绕新目标组织线程；需要延续的模块规则和阶段结果，先写入项目文档。

## 参考资料与适用边界

以下资料提供一种具体产品和模型服务的实践示例，不代表所有工具都采用相同机制。

- [OpenAI：AI 编程工作流最佳实践](https://learn.chatgpt.com/guides/best-practices)
- [OpenAI API：Prompt caching](https://developers.openai.com/api/docs/guides/prompt-caching)

本文提供通用工作方法。自动压缩、跨会话记忆、缓存保留和额度计算等能力，需以实际使用的平台说明为准。
