# Codex 线程管理与缓存使用建议

适用场景：在 Codex 中持续开发一个包含多个模块的项目。

## 1. 推荐原则

**按模块组织工作，按一项连续任务划分线程。**

一个模块可以有多个线程，不必永久绑定一个线程。同一个功能从需求讨论、实现、测试到修复，适合在同一线程中完成；进入另一个独立功能时，可以新开线程。

OpenAI 官方建议，一个线程处理一个连贯的工作单元。如果仍在解决同一个问题，继续原线程有助于保留推理过程和决策背景。

## 2. 同一线程为什么有帮助

需要区分上下文延续和缓存命中。

### 上下文延续

原线程能够延续此前讨论过的需求、约束、设计取舍和排查经过，减少重复解释。这通常是继续使用同一线程最直接的收益。

线程很长时，Codex 会进行上下文压缩，因此不要把聊天历史当作唯一的长期项目记录。

### 缓存命中

缓存复用依赖请求前缀中相同的内容，而不是模块名称或线程名称。

根据官方 API 缓存机制，可以推断：在原线程中连续追加消息、保持前面的内容稳定，有利于缓存复用。但这不是 Codex 桌面端对单个线程命中率的保证。

- 缓存有保留期限，隔几天返回老线程，不代表之前的缓存仍然存在。
- 上下文压缩、摘要或截断可能改变请求前缀，影响缓存复用。
- 实际命中还受模型、请求路由和缓存是否仍可用等因素影响。

**缓存命中率高，不等于总消耗更低。** 很长的线程携带大量历史内容，即使部分命中缓存，也可能比精简的新线程消耗更多。API 缓存价格也不能直接换算成 Codex 套餐额度。

## 3. 什么时候继续，什么时候新开

| 情况 | 建议 | 原因 |
| --- | --- | --- |
| 同一个功能，继续实现、测试或修复 | 继续原线程 | 保留需求与排查背景 |
| 同一个模块，开始另一个独立功能 | 新开线程 | 让上下文围绕新的目标展开 |
| 当前问题尚未解决，但线程已经很长 | 优先保持任务连续，必要时压缩上下文 | 避免丢失未完成问题的关键线索 |
| 一个阶段完成，准备进入下一阶段 | 记录交接信息后新开线程 | 减少下一阶段携带的历史负担 |
| 任务跨越多个模块，但服务于同一个明确目标 | 可以放在同一线程 | 以完整结果划分，而非机械地按目录拆分 |

## 4. 实际示例

以登录模块为例：

- **登录模块：实现账号密码登录并验收**：需求确认、实现、测试和本次修复放在同一线程。
- **登录模块：增加微信登录**：属于另一个独立功能，可以新开线程。
- **登录模块：排查登录状态异常丢失**：作为独立问题新开线程，持续排查到解决。

推荐使用“模块名称 + 本次目标”命名线程，方便查找，例如：

```text
登录模块｜账号密码登录与验收
登录模块｜微信登录接入
登录模块｜登录状态丢失排查
设置模块｜主题切换与持久化
```

## 5. 如何保存模块的长期记忆

**线程负责当前任务的连续性，项目文档负责模块的长期记忆。**

将稳定且有复用价值的信息写入项目文档，例如模块说明、设计决策记录；将需要持续遵守的开发规则写入适当的 AGENTS.md。避免把全部聊天记录堆入规则文件。

阶段结束时，建议记录：

1. 已完成的功能与当前状态。
2. 关键入口文件和模块职责。
3. 已确定的设计决策及原因。
4. 已执行的验证与结果。
5. 尚未解决的问题及下一步。

可直接在当前线程发送：

> 请把本阶段已完成内容、关键文件、设计决策、验证结果和待办整理成项目交接文档，供下一线程读取。区分已验证事实和仍需确认的事项。

新线程开始时可以发送：

> 本次目标是【具体目标】。请先阅读【交接文档路径】及相关代码，核对当前实现，再继续开发。

## 6. 使用优先级

优先保证任务边界清晰、上下文相关、结果可验证，再考虑缓存收益。不要仅为了追求缓存命中率而无限延长线程。

## 官方参考

- [Codex 最佳实践：组织长期对话](https://learn.chatgpt.com/guides/best-practices)
- [OpenAI API：Prompt caching](https://developers.openai.com/api/docs/guides/prompt-caching)

资料核对日期：2026 年 9 月 10 日。本文的线程组织方法属于实践建议；缓存部分依据官方 API 文档，不代表 Codex 桌面端的具体额度计算承诺。
